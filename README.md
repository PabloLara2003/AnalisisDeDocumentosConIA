# Herramienta interna de análisis de documentos

## 1. Configurar variables de entorno

En el backend se necesita al menos la clave de OpenAI.

1. Ir a la carpeta del backend:

```bash
cd backend
```
## 2. Crear el archivo .env :
y agrega tu apikey 
OPENAI_API_KEY= APIKEY ACA

## 3. Ejecuta con Docker desde la raiz del proyecto:
docker compose up --build
correra en: http://localhost:8501

